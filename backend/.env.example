# Supabase configuration
SUPABASE_URL=https://<your-project-ref>.supabase.co
# Server-only. Never ship this key in Android/iOS/React Native apps.
SUPABASE_SERVICE_ROLE_KEY=<your_service_role_key>

# Backend config
MAX_TEXT_CHARS=12000
MAX_URLS_PER_SCAN=15
RISK_THRESHOLD=50
SIGURSCAN_SAFE_MODE=false

# API security
# Production Cloud Run currently requires an app API key at the backend edge.
# Keep the key in Android build config / secure release config, never in source.
REQUIRE_API_KEY=true
SIGURSCAN_API_KEYS=
SIGURSCAN_ADMIN_API_KEYS=
ENABLE_RATE_LIMIT=true
RATE_LIMIT_PER_MINUTE=60
ALLOWED_ORIGINS=https://sigurscan.ro,https://www.sigurscan.ro,https://sigurscan-backend.vercel.app
SIGURSCAN_PUBLIC_API_BASE_URL=https://api.sigurscan.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Server-driven orchestrated scans / preview lane.
# Keep disabled until the Cloud Tasks queue and IAM role are configured.
ORCHESTRATED_CLOUD_TASKS_ENABLED=false
CLOUD_TASKS_PROJECT=
CLOUD_TASKS_LOCATION=europe-west1
CLOUD_TASKS_QUEUE=sigurscan-orchestrated
CLOUD_TASKS_REQUEST_TIMEOUT_SECONDS=4.0
ORCHESTRATED_CLOUD_TASKS_CONTINUE_DELAY_SECONDS=3
# Secret header used by Cloud Tasks when calling /internal/orchestrated/{scan_id}/advance.
# Store in Secret Manager / Cloud Run env, never in source.
SIGURSCAN_INTERNAL_WORKER_TOKEN=

# External services (optional - leave empty to disable)
GEMINI_API_KEY=
# Current GA flash model as of 2025. Valid options: gemini-2.5-flash, gemini-2.5-pro
GEMINI_MODEL=gemini-2.5-flash
# Timeout for Gemini API calls (seconds) - keep tight to avoid blocking polls
GEMINI_TIMEOUT_SECONDS=8.0
MISTRAL_API_KEY=
# Outer timeout for the Mistral AI explanation call (seconds). Must be larger
# than the inner SDK timeout in _call_mistral (requests timeout=8s). On Vercel
# serverless with cold starts, 15s is recommended for production.
# AI_EXPLANATION_TIMEOUT_SECONDS=15
GOOGLE_CLOUD_VISION_API_KEY=
GOOGLE_WEB_RISK_API_KEY=
ENABLE_ASF_INVESTOR_ALERTS=true
# Official ASF investor alerts page. Domains listed as unauthorized investment
# platforms are treated as hard malicious by the backend reputation gate.
ASF_INVESTOR_ALERTS_URL=https://asfromania.ro/ro/a/19/alerte-investitori---informari
ASF_INVESTOR_ALERTS_FEED_TTL_SECONDS=21600
ENABLE_PHISHING_DATABASE=true
PHISHING_DATABASE_DOMAINS_URL=https://raw.githubusercontent.com/Phishing-Database/Phishing.Database/master/phishing-domains-ACTIVE.txt
PHISHING_DATABASE_LINKS_URL=https://phish.co.za/latest/phishing-links-ACTIVE.txt
PHISHING_DATABASE_FEED_TTL_SECONDS=3600
# Optional. URLhaus requires an abuse.ch Auth-Key; leave empty to skip cleanly.
URLHAUS_AUTH_KEY=
# Optional paid provider. Used only for hard/ambiguous invoice issuer checks.
# Keep low: openapi.ro free plan is small and must not be used as a normal fallback.
OPENAPI_RO_API_KEY=
OPENAPI_RO_MONTHLY_BUDGET=100
# Optional paid provider. Used only for hard B2B/email invoice cases.
HUNTER_IO_API_KEY=
HUNTER_IO_MONTHLY_BUDGET=50
HUNTER_IO_TIMEOUT_SECONDS=2.5
SIGURSCAN_URLSCAN_API_KEY=
AI_EXPLAINER_PROVIDER=auto
MISTRAL_MODEL=mistral-small-latest
ENABLE_MISTRAL_SHADOW_ADJUDICATION=false
MISTRAL_ADJUDICATOR_MODEL=mistral-small-2503
MISTRAL_ADJUDICATOR_TIMEOUT_SECONDS=2.5
URLSCAN_VISIBILITY_DEFAULT=unlisted
URLSCAN_TIMEOUT_SECONDS=8.0
URLSCAN_COUNTRY_DEFAULT=ro
URLSCAN_CUSTOM_AGENT=
GOOGLE_WEB_RISK_TIMEOUT_SECONDS=3.0
MAX_WEB_RISK_URLS=20

# Scan latency policy
FAST_REPUTATION_MODE=true
FAST_REPUTATION_INCLUDE_URLHAUS=true
URL_REPUTATION_CACHE_MAX_ITEMS=1000
# Optional server-side pepper for invoice cache keys. Set in Secret Manager/env in production.
INVOICE_CACHE_HMAC_KEY=
ENABLE_DEEP_REPUTATION_FALLBACK=true
ENABLE_CLOUD_AI_EXPLANATION=true
AI_EXPLANATION_TIMEOUT_SECONDS=2.5
ENABLE_OFFER_CLAIM_WEB_CHECK=false
AI_OFFER_CLAIM_TIMEOUT_SECONDS=5.0
OFFER_CLAIM_HTTP_TIMEOUT_SECONDS=3.0
OFFER_CLAIM_GEMINI_MODEL=gemini-2.5-flash
OFFER_CLAIM_GEMINI_TIMEOUT_SECONDS=4.0
OFFER_CLAIM_GEMINI_MAX_OUTPUT_TOKENS=512
OFFER_CLAIM_GEMINI_THINKING_BUDGET=0
OFFER_CLAIM_GEMINI_THINKING_LEVEL=minimal
